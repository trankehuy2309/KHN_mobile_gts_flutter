import 'package:flutter/material.dart';

import '_report.dart';

class ReportAllByDriver extends StatelessWidget {
  const ReportAllByDriver({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return DataNullable();
  }
}
